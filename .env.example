# Backend API base URL (for frontend)
# 
# For DEVELOPMENT (default): Leave empty or set to "/" to use Vite proxy
# The frontend will make same-origin requests like "/api/refresh" which
# Vite will proxy to http://127.0.0.1:8000
# VITE_API_BASE_URL=/
#
# For PRODUCTION: Set to your actual backend URL
# VITE_API_BASE_URL=https://api.yourserver.com
