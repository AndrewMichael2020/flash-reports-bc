# Package marker for the backend package.
# This file intentionally left mostly empty; it only ensures the 'backend' directory
# is a Python package so `python -m uvicorn backend.app.main:app` works from repo root.
__version__ = "0.0.1"
